<%= render 'account/shared/page' do |p| %>
  <% p.content_for :title, t('.section') %>
  <% p.content_for :body do %>
    <%= updates_for @bank_account do %>
      <%= render 'account/shared/box', divider: true do |p| %>
        <% p.content_for :title, t('.header') %>
        <% p.content_for :description do %>
          <%= t('.description') %>
          <%= t('.manage_description') if can? :manage, @bank_account %>
        <% end %>

        <% p.content_for :body do %>
          <% with_attribute_settings object: @bank_account, strategy: :label do %>
            <%= render 'shared/attributes/text', attribute: :name %>
            <%= render 'shared/attributes/text', attribute: :account_number %>
            <%= render 'shared/attributes/text', attribute: :routing_number %>
            <%= render 'shared/attributes/belongs_to', attribute: :ledger_account %>
            <%# 🚅 super scaffolding will insert new fields above this line. %>
          <% end %>
        <% end %>

        <% p.content_for :actions do %>
          <%= link_to t('.buttons.edit'), [:edit, :account, @bank_account], class: first_button_primary if can? :edit, @bank_account %>
          <%# 🚅 super scaffolding will insert new action model buttons above this line. %>
          <%= button_to t('.buttons.destroy'), [:account, @bank_account], method: :delete, class: first_button_primary, data: { confirm: t('.buttons.confirmations.destroy', model_locales(@bank_account)) } if can? :destroy, @bank_account %>
          <%= link_to t('global.buttons.back'), [:account, @company, :bank_accounts], class: first_button_primary %>
        <% end %>

        <% p.content_for :raw_footer do %>
          <%# 🚅 super scaffolding will insert new action model index views above this line. %>
        <% end %>
      <% end %>
    <% end %>

    <%# 🚅 super scaffolding will insert new children above this line. %>
  <% end %>
<% end %>
