<%= render 'account/shared/page' do |p| %>
  <% p.content_for :title, t('.section') %>
  <% p.content_for :body do %>
    <%= updates_for @payroll_run_detail do %>
      <%= render 'account/shared/box', divider: true do |p| %>
        <% p.content_for :title, t('.header') %>
        <% p.content_for :description do %>
          <%= t('.description') %>
          <%= t('.manage_description') if can? :manage, @payroll_run_detail %>
        <% end %>

        <% p.content_for :body do %>
          <% with_attribute_settings object: @payroll_run_detail, strategy: :label do %>
            <%= render 'shared/attributes/belongs_to', attribute: :bank_account %>
            <%= render 'shared/attributes/date', attribute: :check_date %>
            <%= render 'shared/attributes/date', attribute: :pay_date %>
            <%= render 'shared/attributes/belongs_to', attribute: :employee %>
            <%# 🚅 super scaffolding will insert new fields above this line. %>
          <% end %>
        <% end %>

        <% p.content_for :actions do %>
          <%= link_to t('.buttons.edit'), [:edit, :account, @payroll_run_detail], class: first_button_primary if can? :edit, @payroll_run_detail %>
          <%# 🚅 super scaffolding will insert new action model buttons above this line. %>
          <%= button_to t('.buttons.destroy'), [:account, @payroll_run_detail], method: :delete, class: first_button_primary, data: { confirm: t('.buttons.confirmations.destroy', model_locales(@payroll_run_detail)) } if can? :destroy, @payroll_run_detail %>
          <%= link_to t('global.buttons.back'), [:account, @payroll_run, :payroll_run_details], class: first_button_primary %>
        <% end %>

        <% p.content_for :raw_footer do %>
          <%# 🚅 super scaffolding will insert new action model index views above this line. %>
        <% end %>
      <% end %>
    <% end %>

    <%# 🚅 super scaffolding will insert new children above this line. %>
  <% end %>
<% end %>
