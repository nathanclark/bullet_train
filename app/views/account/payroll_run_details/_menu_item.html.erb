<% if can? :read, PayrollRunDetail.new(team: current_team) %>
  <%= render 'account/shared/menu/item', {
    url: main_app.polymorphic_path([:account, current_team, :payroll_run_details]),
    label: t('payroll_run_details.navigation.label'),
  } do |p| %>
    <% p.content_for :icon do %>
      <i class="<%= t('payroll_run_details.navigation.icon') %>"></i>
    <% end %>
  <% end %>
<% end %>
