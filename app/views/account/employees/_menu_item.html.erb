<% if can? :read, Employee.new(team: current_team) %>
  <%= render 'account/shared/menu/item', {
    url: main_app.polymorphic_path([:account, current_team, :employees]),
    label: t('employees.navigation.label'),
  } do |p| %>
    <% p.content_for :icon do %>
      <i class="<%= t('employees.navigation.icon') %>"></i>
    <% end %>
  <% end %>
<% end %>
