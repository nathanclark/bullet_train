<% if can? :read, VendorInvoice.new(team: current_team) %>
  <%= render 'account/shared/menu/item', {
    url: main_app.polymorphic_path([:account, current_team, :vendor_invoices]),
    label: t('vendor_invoices.navigation.label'),
  } do |p| %>
    <% p.content_for :icon do %>
      <i class="<%= t('vendor_invoices.navigation.icon') %>"></i>
    <% end %>
  <% end %>
<% end %>
