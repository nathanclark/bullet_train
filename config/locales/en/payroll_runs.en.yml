en:
  payroll_runs: &payroll_runs
    label: &label Payroll Runs
    breadcrumbs:
      label: *label
    navigation:
      label: *label
      icon: fal fa-puzzle-piece
    buttons: &buttons
      new: Add New Payroll Run
      create: Create Payroll Run
      edit: Edit Payroll Run
      update: Update Payroll Run
      destroy: Remove Payroll Run
      shorthand:
        edit: Edit
        destroy: Delete
      confirmations:
        # TODO customize for your use-case.
        destroy: Are you sure you want to remove %{payroll_run_name}? This will also remove any child resources and can't be undone.
    fields: &fields
      id:
        heading: Payroll Run ID
      company_id:
        heading: Company ID
      schedule:
        _: &schedule Schedule
        label: *schedule
        heading: *schedule
      employees_paid:
        _: &employees_paid Employees Paid
        label: *employees_paid
        heading: *employees_paid
      check_date:
        _: &check_date Check Date
        label: *check_date
        heading: *check_date
      pay_date:
        _: &pay_date Pay Date
        label: *pay_date
        heading: *pay_date
      # 🚅 super scaffolding will insert new fields above this line.
      created_at:
        _: &created_at Added
        label: *created_at
        heading: *created_at
      updated_at:
        _: &updated_at Updated
        label: *updated_at
        heading: *updated_at
    api:
      collection_actions: "Collection Actions for Payroll Runs"
      index: "List Payroll Runs"
      create: "Add a New Payroll Run"
      member_actions: "Actions for an Individual Payroll Run"
      show: "Retrieve a Payroll Run"
      update: "Update a Payroll Run"
      destroy: "Delete a Payroll Run"
      fields: *fields
    index:
      section: "%{companies_possessive} Payroll Runs"
      contexts:
        company:
          header: Payroll Runs
          description: Below is a list of Payroll Runs that have been added for %{company_name}.
          description_empty: No Payroll Runs have been added for %{company_name}.
      fields: *fields
      buttons: *buttons
    show:
      section: "%{payroll_run_name}"
      header: Payroll Run Details
      description: Below are the details we have for %{payroll_run_name}.
      manage_description: You'll also find options for updating these details or removing %{payroll_run_name} from %{company_name} entirely.
      fields: *fields
      buttons: *buttons
    form: &form
      buttons: *buttons
      fields: *fields
    new:
      section: "New Payroll Run for %{company_name}"
      header: New Payroll Run Details
      description: Please provide the details of the new Payroll Run you'd like to add to %{company_name}.
      form: *form
    edit:
      section: "%{payroll_run_name}"
      header: Edit Payroll Run Details
      description: You can update the details or settings for %{payroll_run_name} below.
      form: *form
    notifications:
      created: Payroll Run was successfully created.
      updated: Payroll Run was successfully updated.
      destroyed: Payroll Run was successfully destroyed.
  account:
    payroll_runs: *payroll_runs
  activerecord:
    attributes:
      payroll_run:
        schedule: *schedule
        employees_paid: *employees_paid
        check_date: *check_date
        pay_date: *pay_date
        # 🚅 super scaffolding will insert new activerecord attributes above this line.
        created_at: *created_at
        updated_at: *updated_at
